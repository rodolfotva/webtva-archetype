package ${package};

import org.apache.logging.log4j.LogManager;
import org.apache.logging.log4j.Logger;

public class ${project-name}Test {
  
  private static final Logger logger = LogManager.getLogger(${project-name}Test.class.getName());

}
